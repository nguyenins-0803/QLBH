
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>INS LEATHER</title>
    <link rel="stylesheet" href="~/Content/Site.css" />
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #fff;
            padding: 20px;
            text-align: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #e97b60;
        }

        .search-container {
            margin: 20px 0;
        }

        input[type="text"] {
            padding: 10px;
            width: 300px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        button {
            padding: 10px 15px;
            background-color: #e97b60;
            border: none;
            border-radius: 5px;
            color: white;
            cursor: pointer;
            margin-left: 10px;
        }

            button:hover {
                background-color: #d56a52;
            }

        .product-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            padding: 20px;
        }

        .product {
            border: 1px solid #ddd;
            border-radius: 5px;
            overflow: hidden;
            text-align: center;
            background-color: white;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s;
        }

            .product img {
                width: 100%;
                height: auto;
            }

        .product-title {
            padding: 10px;
            font-size: 16px;
            font-weight: bold;
            color: #333;
        }

        .product-price {
            color: #e97b60;
            font-size: 14px;
        }

        .product:hover {
            transform: scale(1.05);
        }
    </style>
</head>
<body>

    <header>
        <div class="logo">INS LEATHER</div>
        <div class="search-container">
            <input type="text" id="search" placeholder="Tìm kiếm sản phẩm..." />
            <button onclick="searchProduct()">Tìm</button>
        </div>
    </header>

    <div class="product-grid">
        @for (int i = 1; i <= 8; i++)
        {
            <div class="product">
                <img src="@Url.Content("~/Library/dist/img/products/bag" + i + ".jpg")" alt="Túi Xách @(i)" />
                <div class="product-title">Túi Xách @(i)</div>
                <div class="product-price">Giá: @((i * 100000).ToString("N0")) VND</div>
            </div>
        }
    </div>

    <script>
        function searchProduct() {
            const query = document.getElementById("search").value;
            alert("Bạn tìm kiếm: " + query);
            // Thực hiện tìm kiếm ở đây (ví dụ: gửi yêu cầu đến controller để lọc sản phẩm)
        }
    </script>

</body>
</html>
